# Scratch rebuild — 2026-07-07 (REQ-O-002 / REQ-O-006 evidence)

Full reproducibility run: the previous VM was destroyed (`multipass delete
--purge case-poc`) and the environment rebuilt from the repository alone,
following the runbook in `docs/k8s-poc.md` (branch `phase2`, commit `115c129`),
executed by a single operator with no manual intervention.

| Step | Command | Wall clock |
|---|---|---|
| 1. VM + hardened k3s + helm | `bash scripts/vm-up.sh` (launch, Ansible playbook, kubeconfig export) | 119 s |
| 2. Service images (2.0.0) | `bash scripts/images-import.sh` (docker build cached + containerd import) | 26 s |
| 3. GitOps bootstrap | `multipass exec … argocd-install.sh` (Argo CD v3.4.4, secrets bootstrap, root app) | 119 s |
| 4. App converged | Argo CD sync from `origin/phase2` until `Synced/Healthy`, all 5 pods Ready (first-time pulls of postgres/keycloak/artemis images) | ≈ 255 s |
| **Total** | purge → full Phase-2 stack serving traffic | **≈ 8.7 min** |

Notes:

- Step 4 includes one transient CrashLoopBackOff of management-service while
  Keycloak was still importing the realm — recovered without intervention
  (readiness/startup probes + restart policy; this is the designed
  eventual-consistency path, REQ-F-005).
- Runtime credentials were regenerated by `scripts/secrets-bootstrap.sh`
  during step 3; nothing secret was read from the repository (REQ-G-005).
- Subsequent verification on this rebuild: kube-bench (docs/reports/
  kube-bench-2026-07-07.txt), end-to-end demo (`scripts/k8s-demo.sh`), and
  the requirements-catalogue validation report of the same date.
