# Used via ANSIBLE_CONFIG by scripts/vm-up.sh.
# The multipass VM is disposable and its IP/host key change across
# recreations, so TOFU host-key pinning would only produce false alarms.
[defaults]
host_key_checking = False
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
