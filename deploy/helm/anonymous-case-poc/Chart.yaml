apiVersion: v2
name: anonymous-case-poc
description: >-
  Phase-2 anonymous-case POC (submission-service + management-service +
  ActiveMQ Artemis + PostgreSQL + Keycloak) packaged for the CIS-hardened
  k3s cluster. Deployed via Argo CD (deploy/argocd/apps/anonymous-case-poc.yaml);
  runtime credentials come from pre-created Secrets (scripts/secrets-bootstrap.sh).
type: application
version: 0.2.0
appVersion: "2.0.0"
